#include<stdio.h>
#include"function.h"
struct student
{
	int id, birth, tele, age;
	bool sex;
	char from, name;
};

struct student students[100];

void input(int n);
struct student search(int id);

int main()
{

//	sa.sex = 0;
	return 0;
}

void input(int n)
{
	int i;
	for (i = 0; i < n; i++)
	{
		scanf("%d, %d, ...", &students[i].id, )

	}
}

student search(int id)
{
	return student();
}
